---
topic: webapplications-feature-analytics-chart
description: Analytics charts — Recharts, AnalyticsChart, ChartContainer, line/bar charts with theming
---

# Analytics charts (webapplications)

**Also search** the project's rules and skills folders (e.g. `.cursor/rules/`, `.cursor/skills*/`, or project-specific rules) for relevant guidance when implementing.

**Package:** `@salesforce/webapp-template-feature-react-chart-experimental`

Recharts-based line/bar charts with theming. Provides **AnalyticsChart**, **ChartContainer**, and related components.

**To add this feature:** Use **webapplications-feature-adding-features** (install package, copy-then-adjust, read README/AGENT.md from the package). Then apply **webapplications-best-practice** for routes, path aliases, and verification.

**Integration:** After `npm install`, copy from `node_modules/@salesforce/webapp-template-feature-react-chart-experimental` per the package's README/AGENT.md; fix imports to use `@/` and align with app layout and routes.
