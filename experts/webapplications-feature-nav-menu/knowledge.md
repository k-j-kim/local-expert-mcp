---
topic: webapplications-feature-nav-menu
description: Navigation menu — app layout with responsive navigation menu
---

# Navigation menu (webapplications)

**Also search** the project's rules and skills folders (e.g. `.cursor/rules/`, `.cursor/skills*/`, or project-specific rules) for relevant guidance when implementing.

**Package:** `@salesforce/webapp-template-feature-react-nav-menu-experimental`

App layout with responsive navigation menu.

**To add this feature:** Use **webapplications-feature-adding-features** (install package, copy-then-adjust, read README/AGENT.md from the package). Then apply **webapplications-best-practice** for routes, path aliases, and verification.

**Integration:** After `npm install`, copy from `node_modules/@salesforce/webapp-template-feature-react-nav-menu-experimental` per the package's README/AGENT.md; fix imports to use `@/` and align with `AppLayout` and `routes.tsx` (handle for nav).
